/*
  this is just a trick, it is kind of hard to customize the django form widget.
 */
$(document).ready(function() {
    $("label:first").addClass("entry");
    $("ul:first").addClass("entries");
    $("li").addClass("entry");
});
